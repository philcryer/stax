# existing aws keypair name (less the .pem)
STACK_KEY_PAIR="pccrye-20141005"

# instance type: t2.micro, m3.medium, etc
STACK_INSTANCE_TYPE="t2.micro"

# (minimum) number of nodes in cluster
STACK_CLUSTER_SIZE=3

# coreOS channel: stable, beta, alpha
COREOS_CHANNEL="alpha"

# aws region for deploy
STACK_REGION="us-east-1"

# stax prefix
STACK_PRE_NAME="stax"
